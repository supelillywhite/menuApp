<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost, id: :order_cost %>
  </div>

  <div class="field">
    <%= form.label :entree %>
    <%= form.text_field :entree, id: :order_entree %>
  </div>

  <div class="field">
    <%= form.label :sideOne %>
    <%= form.text_field :sideOne, id: :order_sideOne %>
  </div>

  <div class="field">
    <%= form.label :sideTwo %>
    <%= form.text_field :sideTwo, id: :order_sideTwo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
